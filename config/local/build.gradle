apply plugin: 'java'

repositories {
    google()
    jcenter()
    maven { url 'https://developer.huawei.com/repo/' }
}

dependencies { 
}

task copyToLibs(type: Copy) {
    from configurations.runtime
    into 'libs'
}
